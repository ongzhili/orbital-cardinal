import { View } from 'react-native';
import { Text } from 'react-native-paper';
import { SafeAreaView } from "react-native-safe-area-context";


export default function commHome() {
    return (
        <SafeAreaView>
            <Text>this is the Play
            </Text>
        </SafeAreaView>
    );
}